# Stage workflow

The user requires `run-gar.bat` to be reviewed, updated for new setup/startup needs,
and verified in every implementation stage. Keep README launcher instructions current.
Preserve existing `.env`, model selections, workspaces and databases on repeated setup.
The launcher must propagate failures and support paths containing spaces.

The user has authorized Stages 18 through 20 sequentially without approval pauses.
Test and document each stage before beginning the next. Stop after Stage 20.
